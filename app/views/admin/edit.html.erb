    <h1 class="text-center right-pan-title">Edycja danych administratora: "<%= @admin_user.admin_nick %>"</h1>
    <%= form_for(:admin_user, :url => {:action => "update", :id => @admin_user.id}) do |f| %>

    <div class="container table bg-dark" style="width: 40%; border-radius: 5px; padding: 20px;">
      <form>
        <div class="form-group">
          <label>Imię</label>
          <%= f.text_field(:name, class: "form-control") %>
        </div>
        <div class="form-group">
          <label>Nazwisko</label>
          <%= f.text_field(:surename, class: "form-control") %>
        </div>
        <div class="form-group">
          <label>Nazwa użytkownika</label>
          <%= f.text_field(:admin_nick, class: "form-control") %>
        </div>
        <div class="form-group">
          <label>Email</label>
          <%= f.email_field(:email, class: "form-control") %>
        </div>
        <div class="text-center">
          <%= link_to("Wróć", {:action => "index"}, :class => "btn btn-primary") %>
          <%= f.submit("Zapisz zmiany", :class => "btn btn-danger") %>
          <%= link_to("Zmiana hasła", {:action => "password_edit", :id => @admin_user.id}, :class => "btn btn-warning") %>
        </div>
      </form>
    </div>
  <% end %>
